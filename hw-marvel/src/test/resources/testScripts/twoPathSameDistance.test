# Tests pathfinding on a node with 2 path options which have the same length.
# Should print the path with the lexicographically least node (if nodes the same, then
# compares the edges

# Create graph
CreateGraph sameDistance

# Add nodes
AddNode sameDistance a
AddNode sameDistance b

# Add edges
AddEdge sameDistance a b ABa
AddEdge sameDistance a b ABb

# test pathfinding from a to b
FindPath sameDistance a b
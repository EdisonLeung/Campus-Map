# Tests pathfinding for path with least cost but not least jumps

# Create Graph
CreateGraph minPath

# Add nodes
AddNode minPath a
AddNode minPath b
AddNode minPath c

# Add edges
AddEdge minPath a b 5.0
AddEdge minPath b c 5.0
AddEdge minPath a c 20.0

# test pathfinding
FindPath minPath a c
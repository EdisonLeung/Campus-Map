# Tests pathfinding for a node that points to itself

# Create Graph
# ;) heyyy

CreateGraph selfPath

# Add nodes
AddNode selfPath a
AddNode selfPath b

# Add edges
AddEdge selfPath a b 5.0

# test pathfinding
FindPath selfPath a a
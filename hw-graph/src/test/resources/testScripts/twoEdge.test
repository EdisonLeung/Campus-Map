# Creates 2 nodes and Tests creating 2 edges from one node to the other node

# Create graph
CreateGraph twoEdge

# Add 2 nodes
AddNode twoEdge a
AddNode twoEdge b

# adds 2 edges from node a to node b
AddEdge twoEdge a b AB
AddEdge twoEdge a b AB1

# Prints Nodes and children
ListNodes twoEdge
ListChildren twoEdge a
ListChildren twoEdge b
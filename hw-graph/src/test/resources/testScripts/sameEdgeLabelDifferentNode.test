# Tests adding 2 edges with same label to different nodes

# Create graph
CreateGraph sameEdge

# Add nodes
AddNode sameEdge a
AddNode sameEdge b

# adds 2 edges with same label but to different nodes
AddEdge sameEdge a b AB
AddEdge sameEdge b a AB

# Print nodes and children
ListNodes sameEdge
ListChildren sameEdge a
ListChildren sameEdge b